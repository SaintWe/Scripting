# 📱 Scripting 脚本集合

这是一个为 [Scripting](https://apps.apple.com/app/scripting/id6450589951) 应用开发的脚本合集。

---

## 🚀 脚本列表

### 📦 取码器小助手 (Pickup Code Assistant) [@ryanfwy](https://github.com/ryanfwy/scripting)

> 🤖 AI 驱动的取餐码识别工具

**解决的痛点：**
- 外卖、快递取件码需要反复翻找 APP/小程序/短信
- 取餐/取件高峰期手忙脚乱找不到码

**功能特色：**
- 📸 通过截图或照片自动识别取餐码
- 🔔 支持 Live Activity（灵动岛/锁屏）常驻显示
- 🤖 集成 AI 大模型进行智能解析

**使用方式：**
1. 截图或拍照包含取餐码的页面
2. 脚本自动识别并提取取餐码
3. 取餐码会显示在灵动岛或锁屏上

*该脚本为 [@ryanfwy](https://github.com/ryanfwy/scripting) 开发，请前往 [ryanfwy/scripting](https://github.com/ryanfwy/scripting) 获取最新版本，此处为自用修改版*

---

### 🔖 网页书签 (WeBookmarks)

> 📚 快捷网页与脚本启动器

**解决的痛点：**
- 常用网页需要打开浏览器再搜索
- 想快速启动某个脚本但操作繁琐

**功能特色：**
- 🗂️ 支持多文件夹分类管理
- 🎨 自定义 SF Symbol 图标和颜色
- 📲 支持 iOS 分享菜单快速添加书签
- 🔗 支持网页 URL 和脚本启动链接
- 🧩 桌面小组件快速访问
- 📤 数据导入导出

**使用方式：**
1. 添加常用网页或脚本启动链接
2. 自定义图标和颜色
3. 通过小组件一键访问

[![下载](https://img.shields.io/badge/下载脚本-WeBookmarks-green?style=for-the-badge)](https://github.com/SaintWe/Scripting/releases/latest/download/WeBookmarks.scripting)

---

### 🎨 元书皮肤打包 (YuanShuSkinPacker)

> 🖌️ 元书输入法皮肤打包工具

**解决的痛点：**
- 手动打包皮肤文件繁琐
- 需要过滤不必要的文件

**功能特色：**
- 📦 一键打包皮肤文件
- 🗑️ 自动过滤匹配正则的文件
- 👀 打包后直接发送给朋友

**使用方式：**
1. 配置需要排除的文件规则
2. 选择皮肤目录
3. 一键生成皮肤包

[![下载](https://img.shields.io/badge/下载脚本-YuanShuSkinPacker-red?style=for-the-badge)](https://github.com/SaintWe/Scripting/releases/latest/download/YuanShuSkinPacker.scripting)

---

## 🛠️ 开发指南

### 环境准备

1. **安装依赖**
   ```bash
   # 安装 Bun（推荐）
   curl -fsSL https://bun.sh/install | bash
   
   # 安装项目依赖
   bun install
   ```

### 项目结构

```
Scripting/
├── scripts/                    # 脚本目录
│   ├── 脚本名/
│   │   ├── script.json        # 脚本配置文件
│   │   ├── index.tsx          # 主入口
│   │   ├── intent.tsx         # Intent 处理（可选）
│   │   ├── widget.tsx         # 小组件（可选）
│   │   ├── components/        # 组件目录
│   │   └── utils/             # 工具函数
├── dts/                        # TypeScript 类型定义
├── dist/                       # 打包输出目录
├── pack.ts                     # 打包脚本
├── watch.ts                    # 文件监控同步脚本
└── package.json
```

### 开发流程

1. **启动文件监控**
   ```bash
   bun watch.ts
   ```
   监控脚本会自动同步文件到 Scripting 应用的 iCloud 目录。

2. **创建新脚本**
   ```bash
   # 复制现有脚本目录作为模板
   cp -r scripts/WeBookmarks scripts/MyNewScript
   
   # 修改 script.json 配置
   ```

3. **script.json 配置说明**
   ```json
   {
     "name": "脚本名",      // 脚本名称需与目录名一致，仅使用英文以及数字，不含空格
     "version": "1.0.0",    // 版本号，格式为 x.x.x
     "remoteResource": {
       "url": "下载地址",   // 设为 release 的下载地址
       "autoUpdateInterval": 300
     }
   }
   ```

### 打包发布

1. **版本规则**
   - `script.json` 中的版本 patch 必须为 0（如 `1.0.0`、`2.1.0`）
   - 打包时会自动递增 patch 版本

2. **打包命令**
   ```bash
   # 列出所有脚本
   bun pack.ts --list
   
   # 发布模式（智能检测变更）
   bun pack.ts --release --prev-hashes dist/hashes.json
   
   # 强制打包所有脚本
   bun pack.ts --all
   
   # 打包单个脚本
   bun pack.ts "WeBookmarks"
   
   # 测试模式（不修改原文件）
   bun pack.ts --test --release
   ```

3. **发布流程**
   ```bash
   # 1. 提交代码变更
   git add .
   git commit -m "feat: your changes"
   
   # 2. 发布新版本
   bun run release
   ```

### 可复用组件

项目中提供了一些可复用的组件，可直接复制到其他脚本中使用：

- **`action-button.tsx`** - 圆角矩形操作按钮
- **`script-picker.tsx`** - 脚本选择器
- **`sf-symbol-picker/`** - SF Symbol 图标选择器
- **`update-section.tsx`** - 版本更新检查

---

## 📄 版权许可

本项目采用 [MIT License](LICENSE) 开源协议。
